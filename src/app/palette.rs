#![allow(dead_code)]

use iced::Color;

// Catppuccin Latte palette colors
const PALETTE: catppuccin::FlavorColors = catppuccin::PALETTE.latte.colors;

// Accent colors
pub fn rosewater() -> Color {
    PALETTE.rosewater.into()
}
pub fn flamingo() -> Color {
    PALETTE.flamingo.into()
}
pub fn pink() -> Color {
    PALETTE.pink.into()
}
pub fn mauve() -> Color {
    PALETTE.mauve.into()
}
pub fn red() -> Color {
    PALETTE.red.into()
}
pub fn maroon() -> Color {
    PALETTE.maroon.into()
}
pub fn peach() -> Color {
    PALETTE.peach.into()
}
pub fn yellow() -> Color {
    PALETTE.yellow.into()
}
pub fn green() -> Color {
    PALETTE.green.into()
}
pub fn teal() -> Color {
    PALETTE.teal.into()
}
pub fn sky() -> Color {
    PALETTE.sky.into()
}
pub fn sapphire() -> Color {
    PALETTE.sapphire.into()
}
pub fn blue() -> Color {
    PALETTE.blue.into()
}
pub fn lavender() -> Color {
    PALETTE.lavender.into()
}

// Text and neutral colors
pub fn text() -> Color {
    PALETTE.text.into()
}
pub fn subtext1() -> Color {
    PALETTE.subtext1.into()
}
pub fn subtext0() -> Color {
    PALETTE.subtext0.into()
}
pub fn mantle() -> Color {
    PALETTE.mantle.into()
}
pub fn base() -> Color {
    PALETTE.base.into()
}
pub fn surface1() -> Color {
    PALETTE.surface1.into()
}
pub fn overlay2() -> Color {
    PALETTE.overlay2.into()
}
pub fn overlay1() -> Color {
    PALETTE.overlay1.into()
}
pub fn overlay0() -> Color {
    PALETTE.overlay0.into()
}
pub fn surface2() -> Color {
    PALETTE.surface2.into()
}
pub fn surface0() -> Color {
    PALETTE.surface0.into()
}
pub fn crust() -> Color {
    PALETTE.crust.into()
}
